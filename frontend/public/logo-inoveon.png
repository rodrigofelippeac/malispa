<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex">
    <title>404 | Inoveon</title>
    <style>
        /* Fonte Kollektif */
        @font-face {
            font-family: 'Kollektif';
            src: url('/fonts/Kollektif.ttf') format('truetype');
            font-weight: 400;
            font-style: normal;
            font-display: swap;
        }

        @font-face {
            font-family: 'Kollektif';
            src: url('/fonts/Kollektif-Bold.ttf') format('truetype');
            font-weight: 700;
            font-style: normal;
            font-display: swap;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Kollektif', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            background: #ffffff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        .container {
            max-width: 480px;
            text-align: center;
        }

        /* Logo minimalista */
        .logo {
            margin-bottom: 4rem;
            opacity: 0;
            animation: fadeIn 0.6s ease forwards;
        }

        .logo a {
            display: inline-block;
            transition: transform 0.3s ease;
        }

        .logo a:hover {
            transform: scale(1.05);
        }

        .logo img {
            width: 140px;
            height: auto;
        }

        /* Erro 404 - estilo minimalista */
        .error-code {
            font-size: 7rem;
            font-weight: 700;
            color: #005CA9;
            line-height: 1;
            margin-bottom: 1.5rem;
            letter-spacing: -0.02em;
            opacity: 0;
            animation: fadeIn 0.6s ease 0.2s forwards;
        }

        /* Título minimalista */
        h1 {
            font-size: 1.25rem;
            font-weight: 400;
            color: #1a1a1a;
            margin-bottom: 0.75rem;
            letter-spacing: -0.01em;
            opacity: 0;
            animation: fadeIn 0.6s ease 0.4s forwards;
        }

        /* Descrição clean */
        p {
            font-size: 0.9375rem;
            color: #6b7280;
            margin-bottom: 2.5rem;
            line-height: 1.5;
            opacity: 0;
            animation: fadeIn 0.6s ease 0.6s forwards;
        }

        /* Botão minimalista */
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            color: #005CA9;
            font-size: 0.9375rem;
            font-weight: 500;
            text-decoration: none;
            padding: 0.75rem 1.5rem;
            border: 1.5px solid #005CA9;
            border-radius: 6px;
            transition: all 0.3s ease;
            opacity: 0;
            animation: fadeIn 0.6s ease 0.8s forwards;
        }

        .btn:hover {
            background-color: #005CA9;
            color: #ffffff;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(0, 92, 169, 0.15);
        }

        .btn:active {
            transform: translateY(0);
        }

        .btn svg {
            width: 18px;
            height: 18px;
            transition: transform 0.3s ease;
        }

        .btn:hover svg {
            transform: translateX(-3px);
        }

        /* Animação */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsividade */
        @media (max-width: 640px) {
            .logo {
                margin-bottom: 3rem;
            }

            .logo img {
                width: 120px;
            }

            .error-code {
                font-size: 5rem;
            }

            h1 {
                font-size: 1.125rem;
            }

            p {
                font-size: 0.875rem;
            }
        }

        @media (min-width: 768px) {
            .error-code {
                font-size: 8rem;
            }

            h1 {
                font-size: 1.375rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Logo -->
        <div class="logo">
            <a href="/" aria-label="Voltar para página inicial">
                <img src="/logos/logo-azul.png" alt="Inoveon">
            </a>
        </div>

        <!-- Erro 404 -->
        <div class="error-code" aria-hidden="true">404</div>

        <!-- Mensagem -->
        <h1>Página não encontrada</h1>
        <p>A página que você procura não existe ou foi movida.</p>

        <!-- Botão voltar -->
        <a href="/" class="btn">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="m12 19-7-7 7-7" />
                <path d="M19 12H5" />
            </svg>
            Voltar ao início
        </a>
    </div>
</body>
</html>
